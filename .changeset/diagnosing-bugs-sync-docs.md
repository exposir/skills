---
"mattpocock-skills": patch
---

Add an optional Phase 7 to `diagnosing-bugs`: after the fix, sync any docs it touched (dev docs, bug records, changelogs, interface docs), recording the root cause, a cause classification, the fix, validation, leftovers, and format.
